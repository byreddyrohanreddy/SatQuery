# Core dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0

# ML models (runs on CPU, no GPU required)
torch>=2.0.0
transformers>=4.35.0

# LLM routing (optional — system works without it)
anthropic>=0.39.0

# GeoTIFF support (optional — falls back to Pillow if not available)
# rasterio>=1.3.0

# Testing
pytest>=7.4.0
